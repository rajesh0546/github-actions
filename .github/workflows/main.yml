name: github-actions learning

on:
 pull_request:
    branches: [ main ]

jobs:
 build:
  runs-on: ubuntu-latest
  steps:
   - name: checkout repository
     run: echo "This file is running the actions/checkout@v3 to clone the repository."
     uses: actions/checkout@v3